/* write a c program to define a structure data with 3 members day,month,year, Create 2 structure variables 
.Find the elder one amongst the two*/
#include<stdio.h>
void display(struct sample *p)
struct rational
{
	int day;
	int month;
	int year;
};
int main()
{
	struct date d1,d2;
	printf("Enter the date of birth of peerson 1(day:date:Year):");
	scanf("%d%d%d",&d1.day,&d1.mon,&d1.year);
	printf("Enter the date of birth of peerson 1(day:date:Year):");
	scanf("%d%d%d",&d2.day,&d2.mon,&d2.year);
}
